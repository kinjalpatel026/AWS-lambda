version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - run:
          name: Build Artifact
          command: |
            pwd
            ls -al
            gradle build
            cd build/libs
            pwd
            ls -al
            cd ../../..
      - run:
          name: Install packages
          command: sudo apt-get update && sudo apt-get install wget zip unzip -y

      - run:
          name: Zip Artifact
          command: |
            echo "Hello the current build number is ${CIRCLE_BUILD_NUM}"
            pwd
            ls -al
            mkdir -p lambda_artifact
            zip -r csye6225-web-app-${CIRCLE_BUILD_NUM}.zip build/libs/*.jar
            ls -al
            mv csye6225-web-app-${CIRCLE_BUILD_NUM}.zip lambda_artifact/
            ls -al
            pwd
            cd lambda_artifact
            ls -al
            pwd
            cd ..
            pwd
            ls -al

      - run:
          name: Update zip in lambda
          command: |

            aws lambda update-function-code --region=us-east-1 --function-name=csye6225_spring2019  --zip-file fileb://build/libs/*.jar